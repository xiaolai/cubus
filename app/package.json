{
  "name": "cubus-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Cubus desktop shell (Electron) — Web Bluetooth smart cube + camera scanner",
  "main": "main.js",
  "scripts": {
    "prestart": "test -f renderer/vendor/ort-wasm-simd-threaded.jsep.wasm || cp ../cube-scanner/node_modules/onnxruntime-web/dist/ort-wasm-simd-threaded.* renderer/vendor/",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^43.4.1"
  }
}
