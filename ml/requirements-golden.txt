# Pinned dependencies for the golden-frame parity harness (ml/golden_frames.py) in CI.
#
# The harness compares DECODED CLASSES, not raw scores, so it is robust to small numerical drift —
# but the versions are pinned anyway so a CI run reproduces exactly what pinned ml/golden/expected.json
# on the maintainer's machine, and a genuine model/runtime change is the only thing that can move a
# read. Split by platform: Linux CI runs onnx + onnx-int8 + tflite; the macOS runner adds coreml +
# the native (plugin) leg, which also needs the Swift probe built (see .github/workflows/ci.yml).
numpy==2.3.5
pillow==12.3.0
onnxruntime==1.29.0
# Linux + macOS: the Android artefact's runtime.
ai-edge-litert==2.2.0
# macOS only (the coreml leg). pip resolves this on Linux too but the leg is not run there.
coremltools==9.0 ; sys_platform == "darwin"
