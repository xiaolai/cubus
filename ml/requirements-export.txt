# Everything ml/export.py needs to write all four artefacts of the shipped model: the cubedet stack
# plus the CoreML and TFLite converters. The pins are the versions MANIFEST.json `tools` records.
#
#   ml/venv-cubedet/bin/pip install -r ml/requirements-export.txt
-r requirements-cubedet.txt
onnx2tf==1.28.8
# onnx2tf declares no dependencies and imports these unconditionally; without them the TFLite step
# dies on import. Pinned to the versions of the environment that produced the shipped TFLite.
tf_keras==2.19.0
onnx_graphsurgeon==0.6.1
sng4onnx==2.0.1
psutil==7.2.2
tensorflow==2.19.0
coremltools==9.0 ; sys_platform == "darwin"
ai-edge-litert==2.2.0
