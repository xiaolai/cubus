# LEGACY: the Ultralytics (AGPL-3.0) stack that trained and exported v3, the detector this project
# shipped until 2026-09-17. Kept so v3's numbers stay reproducible; nothing that ships uses it now.
# The shipped detector's stack is ml/requirements-cubedet.txt (and ml/requirements-export.txt for
# all four artefacts). Do not install both into one environment: cubedet/train.py refuses to train
# beside ultralytics.
#
# Pinned to the versions that produced v3's artefacts. ml/train.sh runs inside cube-train:1
# (ml/Dockerfile.train), which pins ultralytics/onnxruntime the same way and asserts the pin took.
#
# Re-pin deliberately, when re-baselining -- never incidentally: a newer ultralytics changes the
# TRAINING CODE between a baseline and the experiment meant to be compared against it.
#
#   python3.11 -m venv ml/venv-v3 && ml/venv-v3/bin/pip install -r ml/requirements-train.txt
ultralytics==8.4.126
torch==2.13.0
onnx==1.22.0
onnxruntime==1.29.0
onnx2tf==1.28.8
tensorflow==2.19.0
coremltools==9.0 ; sys_platform == "darwin"
# NO pip could install this file while it said numpy 2.3.5: tensorflow 2.19.0 requires numpy<2.2, and
# on macOS ultralytics 8.4.126 excludes every numpy from 2.0 to 2.3.4. The environments that had both
# got there one install at a time. The only numpy all three accept on macOS is the 1.26 line.
numpy==1.26.4 ; sys_platform == "darwin"
numpy==2.1.3 ; sys_platform != "darwin"
pillow==12.3.0
ai-edge-litert==2.2.0
