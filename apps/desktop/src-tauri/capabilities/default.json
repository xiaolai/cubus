{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Permissions every platform has. Window control is desktop-only and lives in desktop.json.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "cube-vision:default",
    "opener:default"
  ]
}
