{
  "name": "cubus-desktop",
  "version": "0.4.2",
  "private": true,
  "description": "Cubus — Tauri desktop app. Reuses apps/web as the frontend; native BLE via crates/gan-ble.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --features mcp",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
