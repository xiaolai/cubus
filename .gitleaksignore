# gitleaks fingerprints (commit:file:rule:line) of findings that are not secrets, each with its
# reason. The CI `secrets` job fails on anything not listed here.
#
# SUPERSEDED BY `.gitleaks.toml` (2026-09-20), and kept as the record of why. A fingerprint begins
# with a COMMIT SHA, so the history rewrite of 2026-09-20 invalidated every entry below at once and
# a documented non-secret came back as a fresh finding — the nightly went red over a file nobody had
# touched. The exemption now lives in `.gitleaks.toml`, keyed on the path and the rule, which a
# rewrite cannot move. These lines match nothing and are harmless; they are left so the next person
# to wonder why the allowlist moved can read the answer here.
#
# The GAN Gen4 smart cube's fixed AES key, as published by afedotov/gan-web-bluetooth (MIT) and
# carried by every implementation of that protocol, including the one in packages/gan-driver. It is
# a constant of the wire format the cube itself uses, not a credential of this project's; the file
# is the protocol write-up (gan16-driver/ was the package's earlier path).
5d49e3f1d8d946e80cd8cd2beb15c8eaca00dd6f:packages/gan-driver/docs/protocol.md:generic-api-key:69
678102950686f7bc34cb68f37b304642ab6824e1:gan16-driver/docs/protocol.md:generic-api-key:69
